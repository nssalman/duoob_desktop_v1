/// Copy this file to `copilot_secrets.dart` in the same folder and fill in values.
/// `copilot_secrets.dart` is gitignored and must not be committed.
class CopilotSecrets {
  static const String tenantId = 'organizations';
  static const String clientId = 'YOUR_AZURE_CLIENT_ID';
  static const String username = 'YOUR_SERVICE_ACCOUNT_EMAIL';
  static const String clientSecret = 'YOUR_CLIENT_SECRET';
  static const String serviceAccountPassword = 'YOUR_SERVICE_ACCOUNT_PASSWORD';
}
